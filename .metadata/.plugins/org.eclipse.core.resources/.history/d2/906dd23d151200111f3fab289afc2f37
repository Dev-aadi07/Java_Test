package junit;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

import com.Junit.Junit;
//test driven development  -> write the test first then code
class JunitTest {
	Junit j = new Junit();
	@Test
	void revTest() {
		assertEquals("olleh", j.rev("hello"), "good going");
	}

}
